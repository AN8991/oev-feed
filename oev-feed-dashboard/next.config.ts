import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Port 3001 is configured in package.json scripts (dev and start commands)
};

export default nextConfig;
